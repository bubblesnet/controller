
process.env.ENV = "CI"

const ee = require('../src/edge-device-emulator')
const expect = require('chai').expect;

describe("edge-device-emulator",   () => {
    console.log("edge-device-emulator")
    it('edge-device-emulator', function () {
        console.log("closing emulator")
    });
});

